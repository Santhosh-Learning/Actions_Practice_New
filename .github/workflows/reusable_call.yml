name: Reusable Workflow Calling

on:
  workflow_dispatch

jobs:
  ResuableWorkflowCall:
    runs-on: ubuntu-latest
    steps:

      - name: Say Hello during when reusable workflow is called
        uses: ./.github/workflows/reusableworkflow.yml